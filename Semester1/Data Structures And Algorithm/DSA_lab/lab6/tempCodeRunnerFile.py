
#given nodes, with weighted edges
#for each node, we will assign cost {for start node = 0, other = infinite cost {since not yet explored}}.
#find neighbours of each node a ->b,c
#mark a as visited
# find cost of each neighbour {cost from a to node} = cost(a) + dist(a to b)


#find min cost neighbour
#explore mincost neighbour